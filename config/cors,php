<?php

return [
    'paths' => ['web/*', 'sanctum/csrf-cookie'], // Define las rutas que deben soportar CORS
    'allowed_methods' => ['*'], // Permite todos los métodos (GET, POST, etc.)
    'allowed_origins' => [''], // Permite todos los orígenes, cambia '' por una URL específica si lo prefieres
    'allowed_headers' => ['*'], // Permite todos los encabezados
    'exposed_headers' => [],
    'max_age' => 0
];
